<h1>All Products!</h1>

<div class="container">

  <div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-10">

      <table class="table-striped">
        <tr>
          <th>Name</th>
          <th>Price</th>
          <th>Image</th>
          <th>Description</th>
        </tr>
        <% @products.each do |product| %>
        <tr>
          <td><%= product.name %></td>
          <td><%= product.price %></td>
          <td><img src="<%= product.image %>" class="shop-icon"></td>
          <td><%= product.description %></td>
        </tr>
        <% end %>

      </table>

    </div>
  </div>
</div>